info:
  name: Crear Tarea
  type: http
  seq: 1

http:
  method: POST
  url: http://127.0.0.1:8000/tasks/
  body:
    type: json
    data: |-
      {
        "title": "Aprender testing con Bruno",
        "description": "Prueba de contrato HTTP guardada en archivo .bru",
        "completed": false
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
